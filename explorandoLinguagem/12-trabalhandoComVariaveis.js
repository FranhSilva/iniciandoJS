console.log("Trabalhando com Variáveis");

// sempre declarar as variáveis: const, let, var.... não é uma boa prática deixar variáveis sem declarar.

const nome = "Francielle";

//diferença entre const, let e var:

//var: escopo fora do bloco (escopo global); É uma boa prática não utilizar o var, pois declarar variáveis
//em escopo global pode ocasionar alguns problemas;

//let: escopo restrito ao bloco (escopo local); geralmente usamos para contadores ou variáveis que
//poderão ser alteradas mais para frente.

//const: a variável não pode ser atualizada (não permite reatribuição nem redeclaração); geralmente
//usamos essa opção;